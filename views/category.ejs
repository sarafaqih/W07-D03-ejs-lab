<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= name%></title>
</head>
<body>
    <%- include('./partials/nav') %>

    <h1><%= name%></h1>
    <% menuItems.forEach((item) => {%>
            <ul>
                <% if (item.rating === 5){ %>
                <li>Item Name: <%= item.name %> ★ </li>
                           <%}else{%>
            <li>Item Name: <%= item.name %> </li>
<%}%>
                <li>Item Price: <%= item.price %> </li>
                <li>Item Rating: <%= item.rating %> </li>
                <li>Item Details: <%= item.details %> </li>

            </ul>
            <%})%>    


</body>
</html>
